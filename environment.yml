name: workshop-user-image
# This is the standard conda configuration file. Use this file to list
# the conda packages that you need installed in your environment.
channels:
  - conda-forge

dependencies:
  - ipywidgets==8.1.8
  # C# / .NET — provides `dotnet`/`csc` for compiling and running C#.
  # Paired with the Microsoft.dotnet-interactive Jupyter kernel installed in
  # postBuild (the kernel tool targets net10.0, so the SDK major must match).
  - dotnet-sdk==10.0.100
